// Activity 5: Multi-dimensional Arrays

// Task 12: Create a two-dimensional array (matrix) and log the entire array to the console.
let matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

// Logging the entire array to the console
console.log("Two-dimensional array (matrix):", matrix);


// Task 13: Access and log a specific element from the two-dimensional

// For example, accessing the element in the second row and third column (which is 6)
let specificElement = matrix[1][2];

// Logging the specific element to the console
console.log("Specific element at row 2, column 3:", specificElement);